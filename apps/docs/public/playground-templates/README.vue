<script setup lang="ts">

/**
 * NotForm Playground
 *
 * This README is a .vue file because the embedded Vue REPL is an SFC editor.
 * Select it in the file list for a quick reminder of what this playground supports.
 *
 * Supports:
 * - Vue 3 SFCs with TypeScript
 * - Monaco editor with Vue language tooling
 * - Live preview with Tailwind CSS
 * - NotForm core: useNotForm, NotForm, NotField, NotArrayField, NotMessage
 * - Zod via the built-in import map
 * - Editable import maps for browser/CDN dependencies
 * - Shareable playground state through the URL hash
 *
 * Reproduce an issue:
 * 1. Reduce the example to the smallest failing case.
 * 2. Click Share and send the generated playground URL.
 *
 * Test a pull request with pkg.pr.new:
 * 1. Copy the preview package URL from the PR's pkg.pr.new comment, e.g. https://pkg.pr.new/notform@81
 * 2. In the Import Map, set `notform` to the esm.sh PR URL and keep Vue external: https://esm.sh/pr/notform@81?external=vue
 * 3. Re-run the example and share the playground URL with the PR.
 *
 * This playground is for browser-level examples and manual verification. It does not
 * replace the repository's unit/integration test suite or a full local build.
 */
</script>

<template>
  <div> <!-- Documentation-only file. The default entry point is App.vue. --></div>
</template>
