---
title: Quickstart
description: Build a fully typed login form with validation in minutes.
navigation:
  icon: tabler:bolt
---

This guide walks you through building a simple login form using the `useNotForm`{lang="ts-type"} composable and NotForm's headless components.

::tip
Prefer experimenting in your browser with zero setup? You can test ideas directly in the [interactive Playground](/playground).
::

Here is the login form we will be building. Try submitting the form empty, or typing an invalid email, to see the validation in action.

::code-preview
  :::demos-native-quickstart
  :::

#code
  :::code-collapse
    ::docs-demo-code{file="native/quickstart"}
    ::
  :::
::

## Step-by-step

::steps{level="3"}

### Define the Schema

First, define your validation schema using a Standard Schema compliant library (this guide uses `zod`{lang="ts-type"}). The schema describes both fields the login form needs — `email`{lang="ts-type"} and `password`{lang="ts-type"} — along with the rules each must satisfy.

```ts
import { z } from 'zod'

const schema = z.object({
  email: z.email('Enter a valid email'),
  password: z.string('Invalid input').min(8, 'At least 8 characters'),
})
```

This schema is the single source of truth for the form: it decides the shape of `form.values`{lang="ts-type"}, drives which field paths are valid to use with `<NotField>`{lang="vue"}, and produces the validation messages you'll render later with `<NotMessage>`{lang="vue"}.

### Initialize the Form

Use the `useNotForm`{lang="ts-type"} composable to create your form instance. Pass in the `schema`{lang="ts-type"} you just defined and an `onSubmit`{lang="ts-type"} handler that runs once validation succeeds.

```ts
const form = useNotForm({
  async onSubmit(data) {
    // `data` is fully typed as { email: string, password: string }
    console.log('Form submitted:', data)
  },
  schema,
})
```

`form`{lang="ts-type"} is the reactive object every other piece of this guide plugs into — `<NotForm>`{lang="vue"} reads it for context, `<NotField>`{lang="vue"} reads and writes to `form.values`{lang="ts-type"}, and `<NotMessage>`{lang="vue"} reads validation errors from it.

### Render the Form

Use the `<NotForm>`{lang="vue"} component to wrap your form and pass it the `form`{lang="ts-type"} instance via the `form`{lang="ts-type"} prop. `<NotForm>`{lang="vue"} renders a native `<form>`{lang="vue"} element and automatically prevents the browser's default `submit`{lang="ts-type"} and `reset`{lang="ts-type"} behavior, so binding `@submit="form.submit"`{lang="ts-type"} and `@reset="form.reset()"`{lang="ts-type"} is all you need — no `event.preventDefault()`{lang="ts-type"} required.

```vue
<template>
  <NotForm
    :form="form"
    @submit="form.submit"
    @reset="form.reset()"
  >
    <!-- Fields will go here -->

    <button
      type="submit"
      :disabled="form.isSubmitting"
    >
      Submit
    </button>
  </NotForm>
</template>
```

### Connect the Fields

Wrap each input in a `<NotField>`{lang="vue"} component. Its default slot exposes two things you'll use on every field: `path`{lang="ts-type"}, a stable string ID for that field (handy for `<label for>`{lang="vue"}, the input's `id`{lang="ts-type"}, and `<NotMessage>`{lang="vue"}), and `events`{lang="ts-type"}, a set of handlers that track interaction and trigger validation. Spread `events`{lang="ts-type"} onto the input with `v-bind`{lang="ts-type"} — by default this wires up validation on blur and on change, so errors appear once the user leaves the field or edits it, not on every keystroke.

Add both fields from the schema — `email`{lang="ts-type"} and `password`{lang="ts-type"} — the same way:

```vue
<template>
  <NotField
    v-slot="{ events, path }"
    path="email"
  >
    <div>
      <label :for="path">Email</label>

      <input
        :id="path"
        v-model="form.values.email"
        v-bind="events"
        type="email"
      >

      <NotMessage :path="path" />
    </div>
  </NotField>

  <NotField
    v-slot="{ events, path }"
    path="password"
  >
    <div>
      <label :for="path">Password</label>

      <input
        :id="path"
        v-model="form.values.password"
        v-bind="events"
        type="password"
      >

      <NotMessage :path="path" />
    </div>
  </NotField>
</template>
```

  ::note
  `<NotMessage>`{lang="vue"} automatically displays the first active validation error for the given `path`{lang="ts-type"}, and renders nothing when that field has no errors — there's no need to write your own `v-if`{lang="ts-type"} around it.
  ::
::

## Putting it together

With both fields wired in place of the `<!-- Fields will go here -->`{lang="text"} comment from the "Render the Form" step, you have a complete, fully typed, validating login form: submitting with empty or invalid values shows inline errors, and submitting with valid values calls `onSubmit`{lang="ts-type"} with typed, validated data.

## Next Steps

To learn more about how NotForm integrates into your Vue applications, check out the [Components](/docs/components/not-form) guide for the full API of `<NotForm>`{lang="vue"}, `<NotField>`{lang="vue"}, `<NotArrayField>`{lang="vue"}, and `<NotMessage>`{lang="vue"}, or deep dive into [Composables](/docs/composables/use-not-form) to see everything `form`{lang="ts-type"} exposes beyond `submit`{lang="ts-type"} and `reset`{lang="ts-type"}.
