---
title: NotField
description: Renderless field wrapper. Exposes per-field state and event handlers through its default slot.
navigation:
  icon: i-lucide-text-cursor-input
---

`<NotField>` tracks validation, dirty state, and touched state for a single field. It renders nothing itself — everything is handed to the slot and you build the UI.

## Props

::field-group
  ::field{name="path" type="string" required}
  Dot-separated path to this field within `form.values`. Supports nested paths (`address.city`) and array item paths (`tags.0`).
  ::
  ::field{name="validateOn" type="Partial<Record<ValidationTrigger, boolean>>"}
  Per-field overrides for validation triggers. Merged over the form-level config — only the keys you specify are changed.
  ::
  ::field{name="form" type="NotFormInstance<any>"}
  Explicit instance override. Takes priority over any `<NotForm>` ancestor. Required when used outside of `<NotForm>`.
  ::
::

## Slot props

::field-group
  ::field{name="path" type="string"}
  The path passed to this field.
  ::
  ::field{name="value" type="any"}
  Read-only snapshot of the current field value. Do not use with `v-model` — bind `v-model` to `form.values.fieldName` directly.
  ::
  ::field{name="errors" type="StandardSchemaV1.Issue[]"}
  All validation issues for this field from the last validation run.
  ::
  ::field{name="isValid" type="boolean"}
  `true` when this field has no errors.
  ::
  ::field{name="isTouched" type="boolean"}
  `true` after the user has blurred this field, or after the form has been submitted.
  ::
  ::field{name="isDirty" type="boolean"}
  `true` when the current value differs from the initial value.
  ::
  ::field{name="isValidating" type="boolean"}
  `true` while an async validator is running for this field specifically.
  ::
  ::field{name="validate" type="() => Promise<StandardSchemaV1.Result>"}
  Manually triggers field validation. Useful for custom inputs that manage their own interaction events.
  ::
  ::field{name="events" type="{ onBlur, onInput, onChange, onFocus }"}
  All event handlers combined. Spread onto any input with `v-bind="events"`.
  ::
  ::field{name="onBlur" type="() => void"}
  Marks the field touched and triggers validation when `onBlur` is enabled.
  ::
  ::field{name="onInput" type="() => void"}
  Updates dirty state. In eager mode, re-validates while an error exists.
  ::
  ::field{name="onChange" type="() => void"}
  Updates dirty state. In eager mode, re-validates while an error exists.
  ::
  ::field{name="onFocus" type="() => void"}
  Triggers validation when `onFocus` is enabled.
  ::
::

## Usage

### Spread events

The most common pattern. Spread all handlers at once onto a native input.

::code-preview
  ::demos-basic-not-field
  ::

  #code
  ```vue
  <NotField path="email" v-slot="{ events, isTouched, errors }">
    <input
      v-model="form.values.email"
      type="email"
      v-bind="events"
      :class="{ 'border-red-500': isTouched && errors.length }"
    />
    <NotMessage path="email" />
  </NotField>
  ```
::

### Bind individual events

When your component uses different event names, bind handlers individually.

```vue
<NotField path="country" v-slot="{ onBlur, onChange }">
  <CustomSelect
    v-model="form.values.country"
    @close="onBlur"
    @select="onChange"
  />
</NotField>
```

### Nested paths

```vue
<NotField path="address.city" v-slot="{ events }">
  <input v-model="form.values.address.city" v-bind="events" />
  <NotMessage path="address.city" />
</NotField>
```

### Per-field trigger override

Override only the keys you want to change. The rest inherit from the form config.

```vue
<!-- Form has onInput disabled, but this field validates on every keystroke -->
<NotField path="username" :validateOn="{ onInput: true }" v-slot="{ events }">
  <input v-model="form.values.username" v-bind="events" />
</NotField>
```

```vue
<!-- Form has onBlur enabled, but disable it for this checkbox -->
<NotField path="tos" :validateOn="{ onBlur: false }" v-slot="{ events }">
  <input type="checkbox" v-model="form.values.tos" v-bind="events" />
</NotField>
```

### Validate on mount

```vue
<NotField path="email" :validateOn="{ onMount: true }" v-slot="{ events }">
  <input v-model="form.values.email" v-bind="events" />
</NotField>
```

### Conditional UI from slot state

```vue
<NotField path="password" v-slot="{ events, isTouched, errors, isDirty, isValidating }">
  <input v-model="form.values.password" type="password" v-bind="events" />
  <span v-if="isValidating">Checking…</span>
  <span v-else-if="isTouched && errors.length" class="text-red-500">
    {{ errors[0].message }}
  </span>
  <span v-else-if="isDirty && !errors.length" class="text-green-500">
    Looks good
  </span>
</NotField>
```

### Singleton — no `<NotForm>` ancestor

Pass `:form` directly when the field is used outside of a `<NotForm>`.

```vue
<NotField :form="form" path="search" v-slot="{ events }">
  <input v-model="form.values.search" v-bind="events" />
</NotField>
```