---
title: NotMessage
description: Component for displaying validation errors for a specific field.
navigation:
  icon: tabler:code
---

`<NotMessage>`{lang="vue"} is a component for displaying the validation error message for a specific field.

It automatically displays the first active validation error for the field and renders nothing when the field has no validation errors.

::note
Unlike `<NotField>`{lang="vue"} and `<NotArrayField>`{lang="vue"}, `<NotMessage>`{lang="vue"} is not renderless — by default it renders a `<span>`{lang="vue"} element containing the error text, and it can be swapped for a different element or component with the `as`{lang="ts-type"} prop, covered below.

<br><br>

**"Renders nothing when there are no errors"** is literal here: the wrapping element itself is not rendered at all while the field has no active error, rather than being rendered empty. There's nothing in the DOM to select or style until there's a message to show.
::

## Usage

The most common usage is alongside a `<NotField>`{lang="vue"}:

```vue
<template>
  <NotField
    v-slot="{ events, path }"
    path="email"
  >
    <label :for="path">Email</label>

    <input
      :id="path"
      v-model="form.values.email"
      v-bind="events"
      type="email"
    >

    <NotMessage :path="path" />
  </NotField>
</template>
```

::tip
When validation fails, `<NotMessage>`{lang="vue"} displays the first validation error associated with the `email`{lang="ts-type"} field, and nothing when there are no active validation errors.
::

### Displaying Multiple Errors

`<NotMessage>`{lang="vue"} intentionally displays only the first active validation error.

If you need to display every validation issue for a field, use the field's `errors`{lang="ts-type"} slot prop instead:

```vue
<template>
  <NotField
    v-slot="{ events, path, errors }"
    path="email"
  >
    <input
      :id="path"
      v-model="form.values.email"
      v-bind="events"
      type="email"
    >

    <div v-if="errors.length > 0">
      <p
        v-for="(error, index) in errors"
        :key="index"
      >
        {{ error.message }}
      </p>
    </div>
  </NotField>
</template>
```

This gives you the complete list of validation issues instead of only the first message.

## API

### Props

::field-group
  :::field{name="path" type="Paths<TSchema>" required}
  Field path whose first error message is shown.

  <br><br>

  It supports the same dot-separated paths used by `<NotField>`{lang="vue"}:

  ```vue
  <template>
    <NotMessage path="email" />
  </template>
  ```

  For nested fields:

  ```vue
  <template>
    <NotMessage path="user.email" />
  </template>
  ```
  :::

  :::field{name="form" type="NotFormAPI<TSchema>"}
  Form instance. Overrides `<NotForm>`{lang="vue"} inject.

  <br><br>

  By default, `<NotMessage>`{lang="vue"} uses the form instance provided by a surrounding `<NotForm>`{lang="vue"}.

  <br><br>

  You can explicitly provide a form instance when needed:

  ```vue
  <template>
    <NotMessage
      :form="form"
      path="email"
    />
  </template>
  ```

  An explicit `form`{lang="ts-type"} takes priority over the form provided by a `<NotForm>`{lang="vue"} ancestor.

  <br><br>

  This is also required when using `<NotMessage>`{lang="vue"} as a standalone component outside of a `<NotForm>`{lang="vue"}.
  :::

  :::field{name="as" type="Component | string"}
  Root element or component used to render the message. Defaults to `'span'`{lang="ts-type"}.

  <br><br>

  Pass a string tag name to change the rendered element:

  ```vue
  <template>
    <NotMessage
      path="email"
      as="p"
    />
  </template>
  ```

  Or pass a component — useful when your application already has a shared component for validation messages:

  ```vue
  <template>
    <NotMessage
      path="email"
      :as="ErrorMessage"
    />
  </template>
  ```

    ::::caution
    When `as`{lang="ts-type"} is a component, the message is passed as **default slot content**, not as a prop. `ErrorMessage`{lang="ts-type"} in the example above must render its own default `<slot />`{lang="vue"} for the message text to appear — a component that instead expects something like a `message`{lang="ts-type"} prop will render with no visible text. If you need to reshape the message before it reaches a component like that, use the `default`{lang="ts-type"} slot documented below instead of `as`{lang="ts-type"} directly.
    ::::
  :::
::

::note
Any attribute passed to `<NotMessage>`{lang="vue"} that isn't `path`{lang="ts-type"}, `form`{lang="ts-type"}, or `as`{lang="ts-type"} is forwarded to the rendered element — the same way native HTML attributes fall through on any Vue component. This is how you'd attach a `class`{lang="ts-type"}, an `id`{lang="ts-type"}, or a `data-*`{lang="text"} attribute for styling:

```vue
<template>
  <NotMessage
    path="email"
    class="text-sm text-red-500"
  />
</template>
```
::

### Slots

#### `default`{lang="ts-type"}
The default slot receives the current error message.

```vue
<template>
  <NotMessage
    v-slot="slotProps"
    path="email"
  >
    ...
  </NotMessage>
</template>
```

::field-group
  :::field{name="message" type="string | undefined"}
  The first active error message for the specified field.

  <br><br>

  If no error is present for the field, this value is `undefined`{lang="ts-type"}.

  ```vue
  <template>
    <NotMessage
      v-slot="{ message }"
      path="email"
    >
      <span>{{ message }}</span>
    </NotMessage>
  </template>
  ```
    ::::tip
    Because `<NotMessage>`{lang="vue"} already renders nothing when there's no error, you don't need to guard against `message`{lang="ts-type"} being `undefined`{lang="ts-type"} yourself — the slot's content only ever renders while there's a message to show.
    ::::

  Even so, using the slot is useful when you want control over the surrounding markup — for example, adding an icon or custom classes, rather than the plain text `<NotMessage>`{lang="vue"} renders by default:

  ```vue
  <template>
    <NotField
      v-slot="{ events, path }"
      path="email"
    >
      <input
        :id="path"
        v-model="form.values.email"
        v-bind="events"
        type="email"
      >

      <NotMessage
        v-slot="{ message }"
        :path="path"
        as="p"
      >
        <span class="flex items-center gap-2">
          <Icon name="tabler:alert-circle" />
          {{ message }}
        </span>
      </NotMessage>
    </NotField>
  </template>
  ```
  :::
::
