{
  "name": "notform-monorepo",
  "version": "2.1.3",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "description": "Vue Forms Without the Friction",
  "author": "Favour Emeka <favorodera@gmail.com>",
  "license": "MIT",
  "homepage": "https://notformdocs.vercel.app/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/favorodera/notform.git"
  },
  "bugs": {
    "url": "https://github.com/favorodera/notform/issues"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "pnpm lint:root && turbo lint",
    "lint:root": "eslint . --fix --ignore-pattern 'packages/*' --ignore-pattern 'playgrounds/*' --ignore-pattern 'apps/*'",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "test:watch": "turbo test:watch",
    "postinstall": "turbo postinstall",
    "preview": "turbo preview",
    "prepublishOnly": "turbo prepublishOnly",
    "ready": "pnpm install && pnpm lint && pnpm typecheck && pnpm test && pnpm build"
  },
  "devDependencies": {
    "@favorodera/eslint-config": "catalog:tools",
    "@types/node": "catalog:tools",
    "eslint": "catalog:tools",
    "relizy": "catalog:tools",
    "turbo": "catalog:tools",
    "typescript": "catalog:tools"
  }
}
