---
"notform": patch
---

- Refactored `NotForm` props to accept `asChild` and `as` options to to implement dynamic rendering
- Created a standard schema based simple validator for testing and removed all "library inclined" tests
