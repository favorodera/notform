name: CI

on:
  push:
  pull_request:

jobs:
  ci:
    name: Lint, Typecheck, Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10.32.1

      - name: Debug pnpm version
        run: pnpm --version

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      # turbo.json: test -> dependsOn [^build]
      #             build -> dependsOn [^lint, ^typecheck, ^build]
      # turbo resolves the full chain: lint + typecheck -> build -> test
      - name: CI
        run: pnpm test

  preview:
    name: Publish Preview Packages
    needs: ci
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10.32.1

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      # turbo.json: build -> dependsOn [^lint, ^typecheck, ^build]
      # turbo resolves the full chain: lint + typecheck -> build
      - name: Build Packages
        run: pnpm build

      - name: Publish Preview to pkg-pr-new
        run: pnpm dlx pkg-pr-new publish './packages/*'
